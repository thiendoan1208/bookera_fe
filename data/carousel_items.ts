// Configuration for carousel display settings
export const CAROUSEL_CONFIG = {
  // Default number of books to fetch and display per carousel
  DEFAULT_BOOKS_LIMIT: 8,

  // Pagination offset
  DEFAULT_OFFSET: 0,
} as const;
